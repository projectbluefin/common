---
name: dakota-package-go
description: Use when packaging a Go project for dakota/Bluefin BuildStream, when an element needs go_module sources for offline builds, or when setting up GOPATH vendoring in BuildStream
---

# Packaging Go Projects (dakota)

## Powerlevel

- **Level:** 1

Load with: `cat ~/src/skills/dakota-package-go/SKILL.md`

## When to Use

- Packaging a Go project for dakota/Bluefin BuildStream with offline dependency handling
- Choosing the right Go build pattern (vendored GOPATH, go_module sources, vendored tarball)
- Setting up `GOPATH`, `go_module` sources, or `modules.txt` workarounds
- Debugging Go linker flag conflicts or missing build dependencies

## When NOT to Use

- Packaging Rust/Cargo projects → use `dakota-package-rust` instead
- Packaging Zig projects → use `dakota-package-zig` instead
- General BuildStream element reference → use `dakota-buildstream` instead

## Overview

Go projects in BuildStream use one of three patterns for offline dependency handling. The sandbox has no network access, so Go modules must be pre-staged. The Go compiler is provided by `freedesktop-sdk.bst:components/go.bst` (NOT `golang.bst`).

## Pattern Selection

| Condition | Pattern |
|---|---|
| Upstream repo vendors deps + has Makefile | **Pattern 1: make + vendored GOPATH** |
| No vendored deps, few modules (<30) | **Pattern 2: manual/make + go_module sources** |
| No vendored deps, many modules (>30) or no Makefile | **Pattern 3: vendored tarball** |
| Git repo contains `vendor/` directory | Simplest vendored variant of Pattern 3 |

## Common Go Build Environment

All Go elements share these settings:

```yaml
variables:
  optimize-debug: "false"     # Go has its own optimization; BST defaults interfere
  ldflags_defaults: ""         # Disable BST default linker flags that conflict with Go
  strip-binaries: ""           # Go static binaries use their own symbol format

environment:
  GOARCH: "%{go-arch}"         # Maps to amd64/arm64/riscv64 (defined in project.conf)
```

| Variable/Env | Purpose |
|---|---|
| `optimize-debug: "false"` | Prevents BST debug optimization flags conflicting with Go |
| `ldflags_defaults: ""` | Prevents BST default linker flags conflicting with Go's linker |
| `strip-binaries: ""` | Go static binaries use their own symbol format; `strip(1)` breaks them |
| `%{go-arch}` | Architecture mapping: x86_64→amd64, aarch64→arm64, riscv64→riscv64 |
| `MAKEFLAGS: -j1` | For `make` kind — Go parallelizes internally, don't add make-level parallelism |

## Pattern 1: make + Vendored Deps (GOPATH Layout)

Use when the upstream project vendors dependencies in its git repo and has a Makefile.

**Key trick:** Clone the git repo into a GOPATH-style directory using `directory:` on the source, then set `GOPATH` to the build root.

```yaml
kind: make

build-depends:
  - freedesktop-sdk.bst:components/go.bst
  - freedesktop-sdk.bst:public-stacks/buildsystem-make.bst

environment:
  GOPATH: "%{build-root}"
  MAKEFLAGS: -j1

variables:
  ldflags_defaults: ""
  optimize-debug: "false"
  command-subdir: src/github.com/<org>/<repo>

sources:
  - kind: git_repo
    url: github:<org>/<repo>.git
    directory: src/github.com/<org>/<repo>
    track: v*
    ref: <git-describe-ref>
```

**Critical details:**
- `directory: src/github.com/<org>/<repo>` stages the source into the GOPATH layout
- `command-subdir` tells BuildStream to run commands from within that directory
- `GOPATH: "%{build-root}"` makes Go find the vendored deps at the right path
- `MAKEFLAGS: -j1` prevents make from adding parallelism

## Pattern 2: manual/make + go_module Sources

Use when the upstream project doesn't vendor dependencies. Each Go module gets a `go_module` source entry.

```yaml
kind: manual

build-depends:
  - freedesktop-sdk.bst:components/go.bst

variables:
  optimize-debug: "false"

config:
  build-commands:
    - go build -mod=vendor

  install-commands:
    - install -Dm755 <binary> -t "%{install-root}%{bindir}"
    - "%{install-extra}"

sources:
  - kind: git_repo
    url: github:<org>/<repo>.git
    track: v*
    ref: <git-describe-ref>

  - kind: go_module
    url: github:<module-repo>.git
    module: github.com/<org>/<module>
    ref:
      go-version: <version>
      git-ref: <version-tag>-0-g<commit>
      explicit: True
  # ... more go_module entries
```

### modules.txt Workaround

Some projects need a `vendor/modules.txt` file not generated by `go_module`. Add it as a `local` source:

```yaml
sources:
  - kind: git_repo
    ...
  - kind: local
    path: files/<project>/modules.txt
  - kind: go_module
    ...

config:
  build-commands:
    (<):
      - mv -vf modules.txt vendor/modules.txt
```

The `(<):` syntax prepends to default build-commands. Store `modules.txt` in `files/<project>/` in the repo.

## Pattern 3: Vendored Tarball

Use when a vendored tarball already exists or when the number of `go_module` entries would be impractical.

```yaml
kind: manual

build-depends:
  - freedesktop-sdk.bst:components/go.bst
  - freedesktop-sdk.bst:public-stacks/buildsystem-make.bst

environment:
  GOFLAGS: "-mod=vendor"

variables:
  optimize-debug: "false"
  ldflags_defaults: ""
  strip-binaries: ""

config:
  build-commands:
    - go build -o <binary> .

  install-commands:
    - install -Dm755 <binary> -t "%{install-root}%{bindir}"
    - "%{install-extra}"

sources:
  - kind: tar
    url: github_files:<org>/<repo>/releases/download/<version>/<project>-<version>-vendored.tar.xz
    ref: <sha256>
```

### Simplest Vendored Pattern

When the git repo itself contains a `vendor/` directory:

```yaml
kind: manual

build-depends:
  - freedesktop-sdk.bst:components/go.bst

config:
  build-commands:
    - go build ./cmd/<binary>

  install-commands:
    - install -Dt "%{install-root}%{bindir}" -m755 <binary>

sources:
  - kind: git_repo
    url: github:<org>/<repo>.git
    track: v*
    ref: <ref>
```

## CGO Considerations

| Scenario | CGO_ENABLED | Extra Build-Deps |
|---|---|---|
| Pure Go binary (no C deps) | `0` | None |
| Links to C libraries (PAM, libseccomp, etc.) | `1` (default) | `gcc.bst`, `stripper.bst`, library build-deps |

## Common Mistakes

| Mistake | Symptom | Fix |
|---|---|---|
| Using `components/golang.bst` | Element not found | Correct path: `components/go.bst` |
| Missing `GOPATH` for Pattern 1 | `cannot find package` errors | Set `GOPATH: "%{build-root}"` and use `directory:` on git_repo |
| Missing `command-subdir` for Pattern 1 | Build runs from wrong directory | Set `command-subdir: src/github.com/<org>/<repo>` |
| Not setting `MAKEFLAGS: -j1` | Resource exhaustion, flaky builds | Go parallelizes internally; don't add make parallelism |
| Missing `ldflags_defaults: ""` | Linker errors from conflicting flags | Set in variables |
| Missing `strip-binaries: ""` | Go binary stripped, stack traces broken | Set in variables |
| Listing 100+ go_module entries | Works but unmaintainable | Switch to Pattern 3 |
| Missing `modules.txt` with go_module | `go build -mod=vendor` fails | Add as `local` source, move to `vendor/` |
